%% -*- mode: erlang -*-
%% ex: ft=erlang ts=2 sw=2 et
{cover_enabled, true}.

{eunit_opts, [verbose, {report, {eunit_surefire, [{dir, "."}]}}]}.

{erl_opts, [
%%	bin_opt_info,
%%	warn_missing_spec,
]}.

{deps, 
 [
 ]}.

{clean_files, [
    "test/*.beam"
    "erl_crash.dump"
  ]}.

